#include "main.h"

/**
 * add - computes absolute value of a number
 * @a : is the number to be determined.
 * @b : second number
 *
 * Return: returns abs value
 */
int add(int a, int b)
{
return (a + b);
}
